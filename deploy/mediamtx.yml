# Minimal MediaMTX config base: accept published streams on any path.
# At runtime, relay.mediamtx_config sets rtspAddress (from TARGET_PORT) and, when
# TARGET_USERNAME/TARGET_PASSWORD are set, appends an authInternalUsers block.
logLevel: info
rtspAddress: :8554
paths:
  all_others:
